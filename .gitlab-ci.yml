image: maven:latest

stages:
  - build
  - test

build-maven:
  stage: build
  script:
    mvn build

test-maven:
  stage: test
  script:
    mvn test
  artifacts:
    when: always
    reports:
      junit:
        - target/surefire-reports/TEST-*.xml
